# Backend API URL (for server-side API routes)
# In Docker: use the service name (e.g., http://validator-api:3041)
# In local dev: use localhost
API_URL=http://localhost:3041

# API Key for authenticating with the validator service
API_KEY=dev-api-key-for-frontend