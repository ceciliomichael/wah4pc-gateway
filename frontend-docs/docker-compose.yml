services:
  frontend-docs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3036:3036"
    environment:
      - NEXT_PUBLIC_GATEWAY_URL=${NEXT_PUBLIC_GATEWAY_URL:-https://gateway.wah4pc.com}
    restart: always